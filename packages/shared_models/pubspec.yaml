name: shared_models
description: >-
  Canonical ANNEX domain models — analysis, i18n, and user shapes shared by
  the Flutter clients, serialized compatibly with the backend OpenAPI
  contract (ADR-0002).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
