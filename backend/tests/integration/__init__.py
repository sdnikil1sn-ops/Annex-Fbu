"""Integration tests — repositories and adapters against real infrastructure.

These require a reachable PostgreSQL (TEST_DATABASE_URL) and are skipped
otherwise so the default suite stays green without infrastructure.
"""
