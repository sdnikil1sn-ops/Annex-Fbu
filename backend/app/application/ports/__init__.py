"""Ports — the contracts the application layer depends on (ADR-0003).

Ports are Protocol definitions; infrastructure provides concrete
implementations. The application layer never imports infrastructure.
"""
